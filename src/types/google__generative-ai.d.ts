// This file provides augmentations for the @google/generative-ai package
// The main types are already provided by the package itself
// Only add specific augmentations here if needed

// Currently no augmentations needed - removing broad module declaration
// that was interfering with proper type resolution
